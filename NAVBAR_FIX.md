# 修复导航菜单缓存问题

## 问题描述
点击导航"数据源管理"时，页面刷新后，"数据源管理"和"ETL"导航项会互换位置。

## 原因分析
这通常是浏览器缓存导致的：
1. 浏览器缓存了旧版本的HTML或JavaScript代码
2. 不同页面被缓存的版本不一致
3. 导致导航菜单显示混乱

## 解决方案

### 方案一：清除浏览器缓存（推荐快速解决）

**Windows/Chrome**：
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存的图片和文件"
3. 时间范围选择"全部"
4. 点击"清除数据"
5. 刷新页面

**Mac/Safari**：
1. 按 `Cmd + Option + E`
2. 刷新页面

**Firefox**：
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存"
3. 点击"立即清除"

### 方案二：强制刷新（临时方案）

**Windows**：按 `Ctrl + F5`
**Mac**：按 `Cmd + Shift + R`

### 方案三：检查浏览器控制台

1. 按 `F12` 打开开发者工具
2. 切换到 **Network** 标签
3. 勾选 "Disable cache"
4. 刷新页面

这会强制浏览器不使用缓存，重新加载所有资源。

---

## 导航菜单正确顺序

所有页面的导航菜单应该按以下顺序显示：

| 顺序 | 菜单项 | 图标 | 路径 |
|------|--------|------|------|
| 1 | 首页 | 🏠 fa-home | `/` |
| 2 | 资源概览 | 📊 fa-chart-line | `/dashboard` |
| 3 | ETL任务 | 🔄 fa-sync-alt | `/etl` |
| 4 | 数据源管理 | 🖥️ fa-server | `/data-sources` |
| 5 | 元数据浏览 | 📋 fa-table | `/metadata` |
| 6 | 抽取历史 | 📜 fa-history | `/history` |
| 7 | 用户管理 | 👥 fa-users | `/users` |

**当前访问的页面会高亮显示（蓝色背景）**

---

## 验证步骤

清除缓存后，访问各个页面，检查导航菜单顺序是否一致：

1. 访问首页：`/`
2. 访问资源概览：`/dashboard`
3. 访问ETL任务：`/etl`
4. 访问数据源管理：`/data-sources` ← 您发现问题的页面
5. 访问元数据浏览：`/metadata`
6. 访问抽取历史：`/history`

每个页面的导航菜单顺序应该完全一致。

---

## 如果问题仍然存在

### 可能的原因

1. **JavaScript动态修改**：检查是否有JavaScript代码动态修改了导航菜单
2. **CSS影响**：检查CSS是否使用了 `order` 属性或 `flex-direction` 反向
3. **浏览器插件**：禁用所有浏览器插件后重试
4. **代理/VPN**：禁用代理或VPN后重试

### 调试步骤

1. 按 `F12` 打开开发者工具
2. 切换到 **Console** 标签
3. 输入以下代码检查导航菜单：
   ```javascript
   const navMenu = document.getElementById('mainNavMenu');
   const items = navMenu.querySelectorAll('.nav-item');
   items.forEach((item, index) => {
       console.log(`${index + 1}. ${item.textContent.trim()}`);
   });
   ```
4. 检查输出顺序是否正确

---

## 长期解决方案（可选）

如果想彻底解决缓存问题，可以：

### 1. 添加版本号到静态资源

修改模板，在资源链接中添加版本号：

```html
<script src="{{ url_for('static', filename='js/main.js') }}?v=1.2.0"></script>
<link href="{{ url_for('static', filename='css/style.css') }}?v=1.2.0" rel="stylesheet">
```

### 2. 配置服务器缓存策略

在 `app.py` 中添加：
```python
@app.after_request
def add_header(response):
    response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
    response.headers['Pragma'] = 'no-cache'
    response.headers['Expires'] = '0'
    return response
```

### 3. 使用CDN版本控制

如果使用CDN，配置CDN的缓存策略和版本控制。

---

## 快速测试

现在请执行以下操作：

1. **清除浏览器缓存**
2. **强制刷新页面**（Ctrl + F5）
3. **检查导航菜单顺序**

如果顺序正常，问题已解决！
如果仍有问题，请查看浏览器控制台的错误信息。
